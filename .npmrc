# NPM Configuration
# This file helps ensure proper module resolution and installation

# Use exact versions for consistency
save-exact=false

# Prevent hoisting issues
legacy-peer-deps=false

# Ensure platform-specific modules are handled correctly
# This helps with react-native module resolution
scripts-prepend-node-path=true

# Ensure proper workspaces handling
workspaces-update-check=false
